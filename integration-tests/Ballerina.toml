[package]
org = "ballerina"
name = "workflow_tests"
version = "0.1.0"

[[dependency]]
org = "ballerina"
name = "workflow"
version = "0.1.0"
repository = "local"

[build-options]
observabilityIncluded = true
